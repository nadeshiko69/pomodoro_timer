name: pomodoro_timer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.3.3
  cupertino_icons: ^1.0.2
  quiver: ^3.0.1
  share: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.7.0
flutter_icons:
  ios: true
  android: true
  image_path_ios: "lib/icon/icon.png"
  image_path_android: "lib/icon/icon.png"

flutter:
  uses-material-design: true